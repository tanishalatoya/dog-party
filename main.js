/*Target all the elements that you want to manipulate using a variable to store the querySelector of that element

*Items that I want to target:
input
button
h1

Write a function to replace the text in the h1 with the user's
input from the input box

Create an event listener to execute the function once the
"event" happens*/

var someDogs = document.querySelector('#dog-name');
var input = document.querySelector('input');
var button = document.querySelector('button');
